### How does it work
Honestly, the whole algorithm doesn´t fit in my mind, so I was forced to use multiple functions.
I implemented this: the program uses at most 18 + (size of function + size of path structure = 9) * (longest posssible path than can be reached with the algorithm + the second longest) + some variables and argument passes = nearly nothing
